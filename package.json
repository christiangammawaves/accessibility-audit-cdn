{
  "name": "accessibility-audit-unified",
  "version": "13.2.0",
  "private": true,
  "description": "WCAG 2.2 Level AA accessibility auditing system",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build:bundle": "bash build-bundle.sh"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^25.0.0",
    "vitest": "^3.0.0"
  }
}
